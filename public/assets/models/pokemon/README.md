# Pokémon models

The production Pokémon asset is `pikachu.glb`. Select a different partner GLB through `public/assets/scene-config.json`:

```json
"pokemonModel": "assets/models/pokemon/pikachu.glb"
```

The app scales the model to the stage and plays its first embedded animation clip.
