# Pokémon models

Place the final Pokémon or partner GLB files in this folder. Then set `pokemonModel` in `public/assets/scene-config.json` to the chosen file, for example:

```json
"pokemonModel": "assets/models/pokemon/companion.glb"
```

The app scales the model to the stage and plays its first embedded animation clip.
