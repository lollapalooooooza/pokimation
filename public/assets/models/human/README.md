# Human models

The production human asset is `trainer-happy-jump.glb`, a textured rigged character with a 10-second happy-jump animation. Select a different trainer GLB through `public/assets/scene-config.json`:

```json
"humanModel": "assets/models/human/trainer-happy-jump.glb"
```

The app scales the model to the stage and plays its first embedded animation clip.
