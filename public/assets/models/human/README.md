# Human models

Place the final trainer or human-character GLB files in this folder. Then set `humanModel` in `public/assets/scene-config.json` to the chosen file, for example:

```json
"humanModel": "assets/models/human/trainer.glb"
```

The app scales the model to the stage and plays its first embedded animation clip.
